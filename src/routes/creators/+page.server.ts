import { GOOGLE_API_KEY } from '$env/static/private';
import { YoutubeCreator } from '$lib/interfaces/ContentCreator';

export const load = () => {
    const creators = ['@80ProofGoods'];
    return {}
}